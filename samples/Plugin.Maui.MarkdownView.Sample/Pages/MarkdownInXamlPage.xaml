<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mdv="clr-namespace:Plugin.Maui.MarkdownView;assembly=Plugin.Maui.MarkdownView"
             xmlns:mdvs="clr-namespace:Plugin.Maui.MarkdownView.ViewSuppliers;assembly=Plugin.Maui.MarkdownView"
             x:Class="Plugin.Maui.MarkdownView.Sample.Pages.MarkdownInXamlPage"
             Title="MarkdownInXamlPage">

    <ScrollView>
        <!-- xml:space > workaround to keep line-breaks and spacing during HotReload -->
        <mdv:MarkdownView x:Name="MarkdownView" 
                          Margin="6,0,6,0"
                          xml:space="preserve">
            <mdv:MarkdownView.ViewSupplier>
                <mdvs:MauiFormattedTextViewSupplier>
                    <mdvs:MauiFormattedTextViewSupplier.Styles>
                        <mdvs:MauiBasicViewSupplierStyles
                            LayoutViewStyle="{StaticResource MarkdownLayoutViewStyle}"
                            TextViewStyle="{StaticResource MarkdownTextViewStyle}"
                            ListTextViewStyle="{StaticResource MarkdownListTextViewStyle}"
                            BlockquotesTextViewStyle="{StaticResource MarkdownBlockquotesTextViewStyle}"
                            HeaderViewLevel1Style="{StaticResource MarkdownHeaderViewLevel1Style}"
                            HeaderViewLevel2Style="{StaticResource MarkdownHeaderViewLevel2Style}"
                            HeaderViewLevel3Style="{StaticResource MarkdownHeaderViewLevel3Style}"
                            HeaderViewLevel4Style="{StaticResource MarkdownHeaderViewLevel4Style}"
                            HeaderViewLevel5Style="{StaticResource MarkdownHeaderViewLevel5Style}"
                            HeaderViewLevel6Style="{StaticResource MarkdownHeaderViewLevel6Style}"
                            BlockquotesOuterViewStyle="{StaticResource MarkdownBlockquotesOuterViewStyle}"
                            BlockquotesInnerViewStyle="{StaticResource MarkdownBlockquotesInnerViewStyle}"
                            FencedCodeBlockLayoutStyle="{StaticResource MarkdownFencedCodeBlockLayoutStyle}"
                            FencedCodeBlockLabelStyle="{StaticResource MarkdownFencedCodeBlockLabelStyle}"
                            HtmlBlockLayoutStyle="{StaticResource MarkdownHtmlBlockLayoutStyle}"
                            HtmlBlockLabelStyle="{StaticResource MarkdownHtmlBlockLabelStyle}"
                            ImageLayoutViewStyle="{StaticResource MarkdownImageLayoutViewStyle}"
                            ImageSubscriptionViewStyle="{StaticResource MarkdownImageSubscriptionViewStyle}"
                            ImageViewStyle="{StaticResource MarkdownImageViewStyle}"
                            IndentedCodeBlockLabelStyle="{StaticResource MarkdownIndentedCodeBlockLabelStyle}"
                            IndentedCodeBlockLayoutStyle="{StaticResource MarkdownIndentedCodeBlockLayoutStyle}"
                            ListItemBulletViewLevel1Style="{StaticResource MarkdownListItemBulletViewLevel1Style}"
                            ListItemBulletViewLevel2Style="{StaticResource MarkdownListItemBulletViewLevel2Style}"
                            ListItemBulletViewLevel3Style="{StaticResource MarkdownListItemBulletViewLevel3Style}"
                            ListItemBulletViewLevel4Style="{StaticResource MarkdownListItemBulletViewLevel4Style}"
                            ListItemBulletViewLevel5Style="{StaticResource MarkdownListItemBulletViewLevel5Style}"
                            ListItemBulletViewLevel6Style="{StaticResource MarkdownListItemBulletViewLevel6Style}"
                            ListItemViewStyle="{StaticResource MarkdownListItemViewStyle}"
                            ListViewStyle="{StaticResource MarkdownListViewStyle}"
                            ThematicBreakStyle="{StaticResource MarkdownThematicBreakStyle}" />
                    </mdvs:MauiFormattedTextViewSupplier.Styles>
                    <mdvs:MauiFormattedTextViewSupplier.FormattedTextStyles>
                        <mdvs:MauiFormattedTextViewSupplierStyles
                            SpanCodeBackgroundLightColor="{StaticResource MarkdownSpanCodeBackgroundColorLight}"
                            SpanCodeBackgroundDarkColor="{StaticResource MarkdownSpanCodeBackgroundColorDark}"
                            SpanHyperlinkTextLightColor="{StaticResource MarkdownSpanHyperlinkTextColorLight}"
                            SpanHyperlinkTextDarkColor="{StaticResource MarkdownSpanHyperlinkTextColorDark}"
                            SpanTextViewStyle="{StaticResource MarkdownSpanTextViewStyle}"
                            SpanListTextViewStyle="{StaticResource MarkdownSpanListTextViewStyle}"
                            SpanBlockquotesTextViewStyle="{StaticResource MarkdownSpanBlockquotesTextViewStyle}" />
                    </mdvs:MauiFormattedTextViewSupplier.FormattedTextStyles>
                </mdvs:MauiFormattedTextViewSupplier>
            </mdv:MarkdownView.ViewSupplier>
An h1 header
============

Markdown in XAML! 

Paragraphs are separated by a blank line.

2nd paragraph. *Italic*, **bold**, ~~strikethrough~~, and `monospace`. 
Itemized lists look like:

  * item one
  * item *two*
  * last item

Note that --- not considering the asterisk --- the actual text
content starts at 4-columns in.

> Block quotes are
> written **like** so.
>
> They can span multiple paragraphs,
> if you like.

Use 3 dashes for an em-dash. Use 2 dashes for ranges (ex., "it's all
in chapters 12--14"). Three dots ... will be converted to an ellipsis.
Unicode is supported. ☺


An h2 header
------------

Here's a numbered list:

 1. first item
 2. second item
 3. third item

Note again how the actual text starts at 4 columns in (4 characters
from the left side). Here's a code sample:

    # Let me re-iterate ...
    for i in 1 .. 10 { do-something(i) }

As you probably guessed, indented 4 spaces. By the way, instead of
indenting the block, you can use delimited blocks, if you like:

~~~
define foobar() {
    print "Welcome to flavor country!";
}
~~~

(which makes copying and pasting easier). You can optionally mark the
delimited block for Pandoc to syntax highlight it:

~~~python
import time
# Quick, count to ten!
for i in range(10):
    # (but not *too* quick)
    time.sleep(0.5)
    print(i)
~~~



### An h3 header ###

Now a nested list:

 1. First, get these ingredients:

      * carrots
      * celery
      * lentils

 2. Boil some water.

 3. Dump everything in the pot and follow
    this algorithm:

        find wooden spoon
        uncover pot
        stir
        cover pot
        balance wooden spoon precariously on pot handle
        wait 10 minutes
        goto first step (or shut off burner when done)

    Do not bump wooden spoon or it will fall.

Notice again how text always lines up on 4-space indents (including
that last line which continues item 3 above).

#### An h4 header #### 

Here's a link to [a website](http://foo.bar), to a [local
doc](local-doc.html), and to a [section heading in the current
doc](#an-h2-header). Here's a footnote [^1].

[^1]: Some footnote text.

##### An h5 header #####

A horizontal rule follows.

***

Here's a definition list:

apples
  : Good for making applesauce.

oranges
  : Citrus!

tomatoes
  : There's no "e" in tomatoe.

Again, text is indented 4 spaces. (Put a blank line between each
term and  its definition to spread things out more.)

###### An h6 header ######

Here's a "line block" (note how whitespace is honored):

| Line one
|   Line too
| Line tree

and images can be specified like so:

![example image](food_example.jpg "An exemplary image")

Inline math equation: $\omega = d\phi / dt$. Display
math should get its own line like so:

$$I = \int \rho R^{2} dV$$

And note that you can backslash-escape any punctuation characters
which you wish to be displayed literally, ex.: \`foo\`, \*bar\*, etc.
            </mdv:MarkdownView>
    </ScrollView>

</ContentPage>